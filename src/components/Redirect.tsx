import React from 'react'
import {useNavigate, useParams} from 'react-router-dom'

const Redirect:React.FC = () => {
    //const {shortened-url} = useParams()
    console.log(useParams())
  return (
    <div></div>
  )
}

export default Redirect